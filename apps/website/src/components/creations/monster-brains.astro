---
import FlipCreation from "../flip-creation.astro";

import { websiteCreationsMonsterExplosionsPng as glitterSlime } from "@/data/website-images";

type Props = {
  flippable?: boolean;
};

const { flippable = true } = Astro.props;
---

<FlipCreation
  flippable={flippable}
  image={glitterSlime}
  text={["Monster Explosions"]}
  color="green"
/>
