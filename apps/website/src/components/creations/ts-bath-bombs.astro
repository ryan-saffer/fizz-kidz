---
import FlipCreation from "../flip-creation.astro";

import { websiteCreationsTsBathBombsPng as creation } from "@/data/website-images";
type Props = {
  flippable?: boolean;
};

const { flippable = true } = Astro.props;
---

<FlipCreation
  flippable={flippable}
  image={creation}
  text={["'Speak Now' Purple Bath Bombs"]}
  color="yellow"
/>
