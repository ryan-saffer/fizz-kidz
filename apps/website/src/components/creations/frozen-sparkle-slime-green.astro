---
import FlipCreation from "../flip-creation.astro";

import { websiteCreationsFrozenSparkleSlimeGreenPng as creation } from "@/data/website-images";
type Props = {
  flippable?: boolean;
};

const { flippable = true } = Astro.props;
---

<FlipCreation
  flippable={flippable}
  image={creation}
  text={["Frozen Sparkle Slime"]}
  color="green"
/>
