---
import FlipCreation from "../flip-creation.astro";

import { websiteCreationsUnicornBathBombsPng as creation } from "@/data/website-images";
type Props = {
  flippable?: boolean;
};

const { flippable = true } = Astro.props;
---

<FlipCreation
  flippable={flippable}
  image={creation}
  text={["Unicorn Bath Bombs (With horns!)"]}
  color="blue"
/>
