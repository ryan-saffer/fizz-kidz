---
import FlipCreation from "../flip-creation.astro";

import { websiteCreationsSlimeAddInsPng as creation } from "@/data/website-images";
type Props = {
  flippable?: boolean;
};

const { flippable = true } = Astro.props;
---

<FlipCreation
  flippable={flippable}
  image={creation}
  text={["Choose your add ins"]}
  color="white"
/>
