---
import FlipCreation from "../flip-creation.astro";

import { websiteCreationsUnicornBathCrumblePng as creation } from "@/data/website-images";
type Props = {
  flippable?: boolean;
};

const { flippable = true } = Astro.props;
---

<FlipCreation
  flippable={flippable}
  image={creation}
  text={["Unicorn Fizz Crumble"]}
  color="blue"
/>
