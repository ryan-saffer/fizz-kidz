---
import FlipCreation from "../flip-creation.astro";

import { websiteCreationsJellySoapPng as glitterSlime } from "@/data/website-images";

type Props = {
  flippable?: boolean;
};

const { flippable = true } = Astro.props;
---

<FlipCreation
  flippable={flippable}
  image={glitterSlime}
  text={["Jelly Soap"]}
  color="yellow"
/>
